---
import { SITE, NAV_LINKS } from '../data/site';
---

<header class="nav">
  <div class="wrap nav__inner">
    <a href="/" class="brand brand--mark-only" aria-label={SITE.name + ' home'}>
      <img class="brand__logo" src="/pesos-logo.svg" alt={SITE.name} width="1361" height="1257" />
    </a>

    <nav class="nav__links" aria-label="Primary">
      {NAV_LINKS.map((l) => <a class="nav__link" href={l.href}>{l.label}</a>)}
    </nav>

    <div class="nav__right">
      <button class="icon-btn" data-theme-toggle type="button" aria-label="Toggle theme">
        <svg class="icon-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z"/></svg>
        <svg class="icon-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/></svg>
      </button>

      <a class="term-btn" href="#" title="Coming soon" aria-label="Terminal — coming soon">
        <span aria-hidden="true">$</span><span class="term-btn__label">Terminal</span>
      </a>

      <button class="nav__toggle" data-nav-toggle type="button" aria-label="Menu" aria-expanded="false">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
      </button>
    </div>
  </div>

  <div class="nav__drawer" data-nav-drawer>
    {NAV_LINKS.map((l) => <a href={l.href}>{l.label}</a>)}
    <a href="#" title="Coming soon">$ Terminal</a>
  </div>
</header>
